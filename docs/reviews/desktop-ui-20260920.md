# 桌面 UI 检查与修复记录

范围：桌面端 1280×720、1440×900；内置浏览器。检查中的生产账号为测试账号。只读筛选、展开和编辑面板预览不提交业务数据；真实发送、删除、权限变更未执行。

## 第一批修复

| 问题 | 证据 | 处理 |
| --- | --- | --- |
| 标题栏与工作区同色，视觉层次消失 | 生产交易、管理、融资页面截图；用户明确指出 | PageHeader 统一浅蓝底；有标签页保留白色当前标签 |
| 权限筛选没有选中颜色，搜索后当前范围按钮消失 | 生产权限页选择二级债券池，再搜索市场：筛选项消失但仍限制结果 | 范围列表不随搜索消失，选中项复用主按钮颜色；切回全部保留搜索并恢复结果 |
| 交易记录右侧风控和状态列被不必要的固定宽度挤出 | 1440 桌面生产页截图；本地 1280 预览 | 移除 1320px 强制表宽，名称允许换行，区域可键盘滚动；筛选先收缩搜索框，保留选项文字 |

本地生产 CSS 预览已实际确认浅蓝标题栏、权限筛选恢复、1280 桌面交易末列显示。类型、单元及 CI 状态以对应提交记录为准；预览不等同于已部署。

## 已检查的界面与边界

- 门户：匿名/登录后入口与个人入口；登录完成后模块可见。
- 交易总览：指标、图表、导航首屏。
- 交易管理：状态筛选、金额排序，确认两条待确认记录按金额排序；桌面表格。
- 交易流程：普通态、询价展开、编辑模式、节点编辑面板打开/关闭；没有保存配置或修改完成状态。
- 管理我的：资料、邮箱、密码、偏好表单布局；没有保存或发送邮件。
- 管理权限：组合搜索/范围选择、空结果、恢复结果；本地追加浏览器回归。
- 管理通知：联系信息、订阅矩阵、设备入口；没有订阅或开启系统通知。
- 融资仪表盘：生产首屏指标与筛选区。
- 市场热点：生产页面内容与控件树已读取，详情交互尚未完成。
- 市场点评视觉/文字版：初轮审查发现窄桌面内容裁切，需要主代理继续确认并修复。

## 后续优先级

1. 市场点评：复核一级发行/二级行情窄列裁切、指数名称省略；保留已确认报告布局与导出约定。
2. 融资仪表盘：监管面板撑高同排两张指标卡，首屏空白过多；检查与项目、额度、日历的阅读顺序。
3. 交易流程：桌面询价金额占位文字偏窄，编辑面板开启后主图的可达性继续复查。
4. 逐页补齐政策/跟踪点评、研究辅助、二级池、融资模型、授信各页、融资项目/SOP/客户/投资人/数据/负债周报、新闻研报详情和资金日报。
5. 管理后台仅管理员可访问的页面需隔离组件场景；测试账号的不可访问状态不冒充页面视觉验收。

未经实际检查的页面和副作用操作不记为通过。用户后续要求停止子代理，后续检查、修复与交付由主代理直接完成。

## 第二批：操作遮挡与窄桌面报告

- 第一批已通过合并组 `35513721060`，PR #35 合并提交 `a70428d23f8a6cd2e76812cd42ced6cf245b10d4`；Cloudflare 自动构建成功。已在生产融资 SOP 详情刷新，实际确认浅蓝标题栏上线。
- 生产 SOP 列表、SOP 详情和项目页右下角新增按钮与 AI 按钮重叠。桌面融资新增按钮上移到 AI 按钮上方，保留原入口与业务动作；新增真实 SOP 页面及打开、Escape 关闭、焦点恢复场景。
- 1280 桌面市场点评存在指数名省略、较长点位省略、汇总数值裁切以及发行人/债券名截断。保留四列报告架构，完整显示指数名，长机构名称在表内换行；日期、期限和金额保持连续，窄桌面关键数值仍不小于 1rem。用四个指数、非空一级发行和二级行情夹具检查，未改变报告数据与计算。
- 继续检查了生产融资项目列表与项目展开节点、SOP 模板列表和短期融资券 SOP 详情。未修改线上项目、SOP 或提醒。
- 融资仪表盘大留白、询价字段宽度以及其余尚未完成的逐页交互检查继续保留在后续清单，不计为已修复。

## 第三批：融资数据和客户表单

- 第二批 PR #36 已合并，合并组 `35515838435` 与 Cloudflare 构建成功；本轮在生产融资数据页再次确认新增月份按钮与 AI 分离。
- 生产客户搜索后正确显示银行结果，但工具栏搜索按钮另起一行；桌面调整为固定上限、可收缩的一行搜索区。客户编辑中的类型原为无表面原生 select，改为现有 NativeSelect，与其他字段一致。
- 生产融资数据两张模块卡片没有间距；桌面补齐 24px 模块间距。1280/1440 桌面财务指标改为四列，长标题仍允许换行，主值沿卡片底部对齐，备注与网格保留间距。
- 生产月度编辑弹窗缺少 management-page 作用域，既有双列表单样式失效；补回作用域，复用既有样式。内置浏览器已对照生产与本地画面确认双列金额字段和按钮完整可见。
- 本地真实组件与生产 CSS 预览已检查客户默认/编辑、财务非空/月份切换/编辑；财务使用隔离网络夹具，未提交真实业务数据。新增 CI 场景覆盖间距、窄桌面卡片、月份切换、取消及焦点恢复。完整 CI 与上线验收以随后交付记录为准。


## 第四批待验收：筛选和详情空间

- 第三批 PR #38 合并为 `71cda5f37eb4afddc75180839c64bf126d821aeb`，合并组 `35518293457` 和 Cloudflare 构建 `887af38a-508b-4adc-a921-3c02ab5bd241` 成功。内置浏览器重新加载线上融资数据及客户名单，确认卡片间距、四列指标和单行搜索上线。候选首次发现弹窗入场动画中分两次读坐标导致断言误差，改为同一帧测量后候选与严格比较通过，未放宽阈值。
- 继续检查融资仪表盘完整日历、负债详情及投资人页。日历“显示周末”正常切成七列，但预设文字截断；负债详情现金流仅一行时仍被拉高到核心信息等高，右半卡片大量空白。
- 投资人页“具体投资人情况”的债券品种和排序标签在1440桌面被压成逐字竖排；存续规模排序和私募债筛选均正常刷新图表与表格，零存续占比显示横线。拟将标签与选择框组成不可拆分控件组。
- 负债周报生产日期2026-09-20及2026-09-18均为空态，日期切换正常、导出禁用；未点击生成。真实六页报告仍待有归档日期或组件夹具验收，不能用本次空态检查代替。
- 本批已开始实现筛选标签分组、预设自适应宽度以及核心信息旁依次堆叠现金流/完整字段；尚未完成本地画面、回归场景或CI验收，不计为已交付。

## 统一交付约定（用户最新要求）

后续页面检查、修改和本地复核全部积累到 `task/desktop-ui-layout-20260920` 的同一个 PR。完成全部检查和修改后再统一生成最终截图候选、进入合并队列和做上线复查；中途不分批合并。当前本地用1280桌面确认筛选横排、长预设完整、详情卡片排列；投资人环形图补足九类机构夹具，名称保持横排、图例完整换行。排序和私募债空范围已通过内置浏览器复核。

剩余工作以实际界面验收为准：

| 页面组 | 当前证据 | 待完成 |
| --- | --- | --- |
| 交易总览/管理/流程 | 已查首屏、筛选排序、节点打开关闭；既有修复上线 | 最终截图验收 |
| 市场热点 | 本地八热点、范围切换、详情和关闭焦点已查 | 最终截图验收 |
| 政策/跟踪点评 | 本地卡片、类型筛选、关联选择/取消与预览阅读态已查 | 编辑返回内容保留回归、最终截图验收 |
| 研究辅助 | 丰富历史数据、图例、悬浮及指标矩阵已查 | 最终截图验收 |
| 二级池/融资择时 | 二级池非空报告及日期区间；模型六维驱动、因子贡献、悬浮、历史版本、展开和卖方标志已查 | 最终截图验收 |
| 授信总览/日历/周报/助手 | 总览五类分项、日历筛选/翻月、周报非空批复；助手机构选择、空搜索、历史答复与引用展开已查 | 最终截图验收 |
| 融资仪表盘/客户/数据/项目/SOP | 原生产页面；本地项目月/季、展开、新建/编辑、详情时点/时段及成员已查 | 最终复核 |
| 投资人/负债详情 | 本地九类图表、排序、筛选、空范围、重排详情已查 | CI截图最终验收 |
| 负债周报 | 本地非空六页、监管指标、额度表、期限分布、同业续表和利率图已查 | 最终截图验收 |
| 资讯/研报/点评详情及资金日报 | 三类真实详情组件、正文、侧栏、关联跳转；资金日报归档列表已查 | 归档入口及最终截图验收 |
| 管理/个人页 | 本地角色选择/权限比较、个人资料/偏好、通知矩阵/设备、消息重试取消和群发筛选已查 | 错误/空状态及最终截图复核，不扩张测试账号权限 |

本地夹具代表组件视觉和交互，不代表生产接口、写入或真实报告内容验收。兼容跳转不重复算页面；未查状态不记为通过。

研究辅助补入100个日频点、18个月度点及全部指标的合成历史后，内置浏览器发现主利率图和两张利差图在月末/月初挤出重叠日期。时间轴启用重叠刻度避让，本地1280截图确认日期分开；悬浮卡片在面板内显示，图例可切换，价格/需求指标卡标题和曲线完整。该丰富场景新增桌面截图回归，数据仅为测试夹具。

授信总览本地1280检查发现五卡“四加一”和1240px固定表宽，展开详情时机构名称被横向滚动挤出视野。现在五项指标单行，名称/主值顶部对齐，指标标签精简为“额度使用率”；排序控件复用ghost按钮，表格按内容分配列宽，真实溢出时保留键盘滚动。内置浏览器确认机构名称、完整日期、操作列及展开编辑区域同时可见。新增风险筛选/展开收起的桌面回归；现有夹具尚未覆盖全部五类授信分项编辑，后续补丰富数据，不将这次检查视为完整编辑验收。

后续唯一集中 PR 为 #39（draft）。本地检查和提交继续累积于此，不自动合并，不提前触发最终基线/上线验收。

## 同一 PR 持续检查：2026-09-21

- 授信编辑补齐五类分项后发现“四加一”和字段纵向拉伸。桌面五分项同排、字段按内容紧凑排列；基础信息按身份四列、金额/日期/经办信息三列分组。1280本地实际确认；周报五项指标也在1280保持单排，非空批复明细完整显示。
- 热点范围的 Tabs 实际输出 `data-state="active"`，原选择器 `data-active` 未命中，导致当前项没有颜色。改用正确状态选择器与既有蓝色选中表面。热点详情桌面扩至42rem，短说明、影响、驱动与首条证据同屏；移除冗余英文小字，日期输入恢复正常字重。范围切换与详情打开关闭均已目视检查。
- 政策关联复选框受页面通用 button 内边距影响而变成长条；排除 checkbox 后恢复方形。已检查选择数量变化、取消后原关联不变、财政类型查询。
- 跟踪点评预览原先仍把元数据编辑表单放在正文前，挤占首屏。预览隐藏该表单，正文单列阅读；1600以下编辑内容与证据纵向排列。浏览器确认预览正文进入首屏；返回编辑的数据保留已写回归断言，尚待执行完整浏览器测试。
- 二级池日期选择原先起始日没有 endpoint 类，整月按钮也缺少明确区间颜色。修正条件优先级，起止日主色、区间浅蓝连续底色，并提供按下状态。实际选择9月10日至15日后重新打开，标题范围、端点及区间正确。图表右轴标题改为向左延伸并下移主绘图区，1280画面已确认标题与图例不再重叠或裁切。
- 本地视觉构建遗漏静态目录，造成机构标志变成破图；补复制生产静态资源后，浏览器确认兴业、天风、中信标志正常。此项是预览环境一致性修复，不记为生产标志故障。
- 本轮 `check:quick`、生产构建、视觉构建、12项相关单元/视觉工具测试和候选覆盖清单检查通过。新增桌面弹层、日期选择、卖方标志与五类授信分项回归场景；这些新增截图尚未生成，完整浏览器CI未执行，不计为最终验收。

所有数据均为只读本地合成夹具。本轮未检查线上新发布、未合并 PR，也未生成最终截图基线。


## 同一 PR 持续检查：助手、详情与六页报告

- 融资择时补足六维驱动、五因子和两日期版本，1280内置浏览器确认雷达标签、悬浮信息、因子名称完整；切换历史版本后业务指标恢复显示。
- 授信助手输入区原有白色外框内再套灰色圆角表面，空白时近似禁用控件。输入区改为透明内层，补简短占位标签，保留外框焦点状态。已实际检查机构搜索、无结果、方向键选中、回车恢复历史、引用展开及新输入的按钮启用条件；未发送真实问题。
- 研报、新闻及点评详情接入真实页面组件与生产CSS。发现点评长标题和关联政策标题被省略号裁切，统一允许完整换行；无关联政策的点评改为900px居中阅读区。恢复正文有序/无序列表标记和章节字重。1280截图已确认长标题、关联侧栏、列表、表格及独立点评布局。新闻的关联链接实际跳转到对应政策锚点。
- 资金日报归档页日期、大小、发布时间和打开入口排布正常；此次未打开上传文件选择器，也未把任意用户上传的HTML内容计为网站UI模板验收。
- 非空负债周报使用真实融资布局和报告组件，补69个月余额、五类发行品种、期限分布、14条同业申报和100日利率夹具，逐页检查六页。保持原A4页面结构。第三页发现年度余额/利率标签重叠及发行末值挤到右轴；标签分别上下排列、边缘向内对齐，发行末值移至点左侧并只渲染最后一个有效点。第六页原日期仅显示月份，导致一月出现两次相同标签，改成月/日并保留重叠避让。
- 发行图例原来只隐藏柱体，对应利率线仍显示；同品种柱体与利率线改用共同图例名称。其它带合计标注的堆积图筛选状态仍需补查，不将正常态截图当作全部交互验收。
- 本轮新增助手、三类详情和六页报告的桌面回归场景。类型问题集中在新夹具从空数组推断为never及真实布局所需字段，已用完整对象结构与字段修正，没有关闭检查。快速检查、生产与视觉构建、4项助手/标题栏测试和25项负债周报测试通过；新增浏览器场景仍待最终候选统一执行。


## 同一 PR 持续检查：管理页与融资项目

- 管理员角色目录的当前项只有失效的 `is-selected` 类，没有可见选中颜色；改用现有主色按钮，并把“只读”徽标放回角色标题同一行。实际切换研究角色后保留业务范围筛选，可比较该角色未授权的操作。
- 通知订阅表的复选框是块级 flex 元素，原本没有跟随单元格居中，三列控件与标题错位。补居中后已目视确认列对齐及勾选反馈。桌面工作区底部预留悬浮AI操作空间，滚动到末尾时设备移除按钮完整露出；聊天、流程画布和独立报告不套用该间距，已有浮动新增页面继续保留104px。
- 个人资料四张卡片改为纵向布局并将操作区对齐底部，去掉Card内部默认gap造成的重复间距；行情偏好横排，退出入口放左侧，避开悬浮AI。实际检查未保存的偏好选择、邮箱确认后原邮箱按钮仍禁用，没有保存账号或发送邮件。
- 消息投递的时间允许日期/时间自然分行，渠道和中文来源分行，收件人保留最小阅读宽度；短邮箱不再拆成两行，长标题完整换行契约保持。已检查不确定结果重试弹窗并取消，以及群发选择两人后搜索单人仍保留两人计数，未发送消息。工作台统一输入字重扩展到Maia组件的实际data-slot，避免控件内容继承外层标签粗体。
- 融资项目月/季按钮也只有失效选中类，改为主色选中并提供按下状态；新建与编辑弹窗统一560px桌面宽度和可识别名称。项目季度刻度、展开节点、时点/时段、未安排日期、新增节点时段字段均已在1280浏览器检查。
- 项目详情成员容器与成员行被同一flex规则横排，窄侧栏内姓名和邮箱逐字换行。改为纵向成员列表，内部保留头像/文字横排，重复职责去重；浏览器确认两名成员的完整姓名、职责与邮箱。
- 本轮管理与项目场景接入真实页面组件；项目Superforms依赖的page store增加只读harness适配，服务器动作和动作反序列化明确不可用。只对控件、草稿与布局验收，不以这些场景宣称写入或服务端表单校验已验收。
- `check:quick`、生产构建、视觉构建及14项相关轻量测试通过，候选覆盖清单通过。新增管理/项目截图回归未执行最终截图候选；仍保持同一草稿PR，未合并或检查线上新发布。


## 同一 PR 持续检查：仪表盘与 SOP 详情

- 融资仪表盘原监管标题与上限换行，撑高第二排并留下大块空白。监管标题缩为简洁标签、完整名称保留在悬浮标题中，六张卡片按相同行高排列；1280桌面已目视确认主值和四项监管指标完整。
- 存量结构使用九类数据复核后，增加图表高度并启用紧凑标签布局；图例保持完整换行。项目和额度表精简列名、在模块标题显示“亿元”，金额/期限/日期保持连续。1280浏览器确认30.00、200.00和完整日期不拆行。
- 日历单日事件只在格内显示前三项，其余通过“更多 N 项”打开当日完整列表。实际检查五/七列切换、六事件展开、Escape关闭及焦点返回；弹层事件至少44px高。试算30亿元通过、130亿元超额及预设筛选后的410亿元余额均为合成夹具交互证据，不代表业务数据核验。
- SOP新增节点打开时触发Svelte props_invalid_value：绑定Input.ref的初值为undefined，与组件null默认值冲突。改为明确null初始化，弹层挂载后聚焦名称；补齐管理表单作用域及36rem宽度。实际打开后切换时段、输入-7/-1、显示T-7至T-1并取消，未提交写入。
- SOP键盘抓取节点原没有可见选中状态；现在按钮与节点背景均高亮。实际Space抓取、方向键移动、Escape撤销顺序。提醒历史已检查加载更多、失败筛选、搜索空态及清除；入队状态使用浅蓝徽标，投递链接有主色和下划线，目标类型显示中文。
- 快速检查、生产构建、视觉构建通过；SOP/排序/提醒13项、日历/仪表盘8项轻量测试通过。既有布局测试原固定断言不等高行，随此次等高设计更新；新增浏览器场景还会测量真实卡片高度、数字行数及表格溢出。候选覆盖清单和新测试语法检查通过，最终截图候选与完整浏览器CI尚未执行。


## 同一 PR 持续检查：报告图例合计

- 非空负债周报可比券商图隐藏公募债后，原合计232仍留在已经缩短的180亿元柱体外；到期分布共用相同组件。合计改为仅计算当前显示品种，并将图例选中状态传回图表，避免重绘复原选择。全取消时保留图表与图例，零合计不绘制标签，支持重新开启品种。
- 为图例切换、全取消和恢复补充组件浏览器断言，以及未显示品种不参与合计、分类顺序和全取消的轻量单元测试。最终浏览器CI仍与全站截图候选统一执行。

- 内置浏览器复核横向图隐藏公募债后232变180，全取消后图例保留，重新开启公募债恢复52；纵向月度图20变18，标签贴合柱顶。关闭跨系列淡化，避免取消图例后剩余柱体与合计文字持续低对比。生产/视觉构建与两项相关轻量测试通过，浏览器CI未执行。


## 同一 PR 持续检查：提醒规则与逐日台账

- SOP提醒规则与新建模板弹窗也缺少管理表单作用域，字段挤在一起；补齐作用域，统一36rem宽度、可识别标题并去掉重复英文标签。实际检查两周期、节点选择、指定邮箱、删除周期、新建模板、取消和Escape。取消后原先节点/邮箱清空但周期/收件人模式残留，现统一重置默认草稿。未保存规则、创建SOP或发送邮件。
- 逐日台账仍有独立可达页面，不能由二级池周报替代验收。补60日历史、六类持仓、五笔成交及有/无原始文件的只读夹具；六卡改为普通桌面三列两排、宽桌面六列，填补原四加二的空位。
- 台账持仓饼图原侧图例和引线文字重叠并出现省略号；图例移到底部，名称与占比分行，完整显示六类名称。期限图八项标签缩至适合列宽的12px，最长标签分行。全部账户的收益率右轴原固定0–4%，超过上限的曲线被裁切；改为根据有效值扩展范围，浏览器确认超过5%的峰值完整显示，日期刻度互不重叠。
- 台账管理原选中日期只有失效selected类；使用主色选中按钮、按下状态和方形日期网格。已检查9月15日有原始文件、14日仅数据库记录、账户范围切换和完整成交金额/日期，未执行上传、下载或删除。
- 本轮快速检查、生产/视觉构建、31项台账/提醒轻量测试及候选覆盖清单通过。新增台账和多周期提醒的浏览器回归尚未执行最终候选；覆盖清单已无deferred项，仅表示场景已登记，不代表最终截图或完整CI已通过。


## 同一 PR 持续检查：助手状态、详情恢复与预览结构

- 检查助手短对话时发现本地harness的普通#app容器使100%页面高度失去参照。生产app.html使用display:contents；预览现在采用相同容器结构，不添加替代生产CSS。修正后1280×900下工作台和输入区底边均为900px，助手输入区在底部。此项是预览一致性修复，不作为生产布局缺陷；最终桌面复核与候选截图必须基于修正后的容器。
- 本地模拟助手处理中会话，实际检查全局进行中面板、关闭面板、展开处理记录、第二轮检索与最新复核记录的内部滚动；客户选择/新对话/发送在处理中正确禁用。模拟失败后重新发送恢复同一问题的答复；历史连接失败后重新连接保留客户选择并恢复引用。请求仅发送到本地合成SSE，不调用AI或生产接口。
- 重试结果原在AI面板展示完整JSON，包括会话、内部字段与业务Schema。AI请求增加独立结果展示文本，结构化返回值保持原样供业务页面使用。授信使用现有答复与来源文本，市场点评/热点/跟踪点评/融资卖方观点由各自调用页提供正文。浏览器确认授信正文、待确认事项和资料来源清晰显示，内部字段不再出现在结果面板。
- 新闻、研报、研究点评详情均以首次503、重试返回正文的本地场景检查，错误提示与重新读取按钮可达，恢复后正文及关联政策完整。新增对应重试断言和统一错误截图；没有改写原始资料。
- 快速检查、生产/视觉构建与11项AI传输/工作台生命周期测试通过；既有生命周期测试仍验证结构化返回值，并增加面板展示正文而非JSON的断言。助手处理/失败/可读结果及详情重试截图已登记，最终候选与全量浏览器CI仍未执行。


## 同一 PR 持续检查：交易流程与周报快讯

- 拆借询价五列中“金额（亿）”被截断，显示占位改为“金额/亿”，保留原完整无障碍名称；已填字段增加原值悬浮标题。实际检查五字段填写、Tab追加空行、历史候选和金额/期限/价格显示，未向生产写入交易。
- 节点编辑前序链接原禁止换行，长流程名称溢出侧栏；改为左对齐自然换行。侧栏打开后主图变窄，选中节点原被裁切；现在自动滚动到选中节点，并复用共享可键盘滚动区域。浏览器确认切换到右侧流程节点仍完整可见，关闭编辑侧栏后焦点回到该节点。
- 高级“节点设置”沿用旧collapse类，实际计算样式为visibility:collapse，入口不可点击。去掉冲突类并给原生details补齐44px标题与内容间距；实际打开图标、起止时间、节点类型、父分支、偏移和恢复布局控件。侧栏底部增加操作余量，最末按钮不会被AI悬浮入口盖住。
- 授信周报已复核非空本周快讯、扩额金额和到期日、五类批复明细、分组授信表，1280桌面全部完整显示。本次不调整授信口径。
- 快速检查、生产/视觉构建及19项流程/边线/键盘滚动测试通过。首次DOM测试发现测试环境没有全局CSS.escape，改用节点data属性匹配完成相同定位，未关闭检查。新增询价占位、节点编辑、高级设置和非空周报截图场景，待统一候选验收。


## 最终候选验收进行中

- 已同步主分支云资源优化提交，未出现业务或样式冲突。候选运行35532131046针对31f5e465f85d4fb66e950d5f8c2534a8772e8e57，123项通过、3项失败、36项按桌面范围及既有规则跳过；整体失败，未导入任何基线。
- 已下载失败日志、截图与trace一次。融资卡在CI字体/滚动条宽度下将“140.80亿元”拆行，高度214px超过原210px验收约束；细项标签简化为“短债”，金额和单位不可拆行。标题仍完整显示“短期负债 / 净资本”，数据与口径不变。1264px本地压力宽度下六卡均177px，四项完整金额正常，未放宽高度阈值。
- 助手重试断言原同时匹配正在关闭的结果面板和聊天正文，现限定“对话记录”区域；研究图表断言原把图表host及内部SVG都算成img，现分别验证三个具名图表。没有删除交互、屏蔽画面或调整截图容差。修正后快速检查、生产/视觉构建及清单检查通过，准备重新生成当前源码候选。
- 修正后的预览结构中，1440桌面复核了财务四列指标与编辑、客户搜索表格、个人资料四卡、通知矩阵和底部设备按钮、热点画布及融资仪表盘。财务仍使用只读隔离响应，未连接真实数据后台或保存配置。

- 第二轮候选35532991558（7e0cb1394d6fc310067b30374ceb1b6acbf7a88e）生成与交互门禁成功，但尚未导入。审阅上一轮相同截图场景时发现，A4报告、完整授信展开区与融资日历超过900px视口，被真实滚动容器裁切；高级设置截图还夹入悬浮AI入口。它们不能作为完整内容证据。
- 保留1280×900的交互与布局检查，只在拍摄完整长内容时临时使用1280×1600桌面视口，并新增整个目标区域100%进入视口的断言，拍摄后恢复900px继续交互。不隐藏标题栏/按钮、不修改组件样式、不屏蔽业务区域、不提高容差。基线继续等待修正后的当前源码候选。

- 最终范围审计补查了仓库内实际渲染的邮件验证提示页：品牌返回、标题和继续登录入口均在本地桌面确认；仅验证链接目标，不提交凭据或触发认证流程。该静态页面已加入真实组件harness与截图场景。profile和management入口经源码确认仅303跳转到已检查的management/me，其他兼容URL复用已检查工作台，不重复计作独立页面；修正了覆盖清单中的旧说明。

- 第三轮35533459313为124通过、2失败；整页第三页报告与高级设置截图已能完整显示。授信展开区和第四页报告的自动滚动仅贴齐视口边缘，IntersectionObserver仍报告0.999035/0.999977的可见比例。改为将捕获目标居中滚动，保留100%可见断言，避免固定标题栏和悬浮按钮进入局部截图；不以降低可见比例或截图阈值消除失败。


## 最终截图审阅与入队准备

- 当前源码 `1a959d9d7a97494c58e8477f095d956a10f5b94d` 的[候选运行35533980375](https://github.com/fin-research/dashboard/actions/runs/35533980375)通过。核对工件源码SHA和126张PNG的清单/校验和后导入；114张为新增或预期变化，12张保持逐字节一致。历史darwin基线不改。
- 已逐张审阅变化的桌面图及既有CI手机图；其中总览、交易、第三页负债报告、两张投资人图与两张台账图还与已审阅第三轮工件逐字节一致。手机仅做已有回归工件核对，没有扩大为手机交互审计。
- 桌面审阅涵盖工作台色彩层次、非空/空态/失败恢复、筛选和选中态、编辑与阅读、流程分支和高级设置、助手处理中/失败/结果、六页负债报告与长表格。完整报告/展开区域的100%可见断言已通过；普通页面截图仍只证明当前滚动位置，底部操作可达性由此前内置浏览器滚动与交互断言补充，不将首屏图表述成全部内容截图。
- 本次只导入当前源码成功候选，未导入前三轮失败或未审候选，未调整截图100像素容差、屏蔽业务区域或删除交互检查。最新origin/main仍为cdc0c98，已包含在任务分支。所有修改汇集PR #39；后续仅剩合并组完整CI和合并后的部署/线上复查，当前尚未宣称完成这些步骤。

- 首次合并组35534621459（78e83b3c4af31990b0cbbea479e171d420dc41dc）的构建、浏览器交互和严格截图比较通过；完整单元检查因授信旧标签源码断言失败，整体验收失败且未合并。卡片已经按本次设计使用“额度使用率”，图表仍保留完整标题；更新原断言并同时验证原utilization数值与百分比单位，未修改页面或基线。22项授信回归和快速检查通过后重新入队。
