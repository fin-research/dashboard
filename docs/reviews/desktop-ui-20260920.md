# 桌面 UI 检查与修复记录

范围：桌面端 1280×720、1440×900；内置浏览器。检查中的生产账号为测试账号。只读筛选、展开和编辑面板预览不提交业务数据；真实发送、删除、权限变更未执行。

## 第一批修复

| 问题 | 证据 | 处理 |
| --- | --- | --- |
| 标题栏与工作区同色，视觉层次消失 | 生产交易、管理、融资页面截图；用户明确指出 | PageHeader 统一浅蓝底；有标签页保留白色当前标签 |
| 权限筛选没有选中颜色，搜索后当前范围按钮消失 | 生产权限页选择二级债券池，再搜索市场：筛选项消失但仍限制结果 | 范围列表不随搜索消失，选中项复用主按钮颜色；切回全部保留搜索并恢复结果 |
| 交易记录右侧风控和状态列被不必要的固定宽度挤出 | 1440 桌面生产页截图；本地 1280 预览 | 移除 1320px 强制表宽，名称允许换行，区域可键盘滚动；筛选先收缩搜索框，保留选项文字 |

本地生产 CSS 预览已实际确认浅蓝标题栏、权限筛选恢复、1280 桌面交易末列显示。类型、单元及 CI 状态以对应提交记录为准；预览不等同于已部署。

## 已检查的界面与边界

- 门户：匿名/登录后入口与个人入口；登录完成后模块可见。
- 交易总览：指标、图表、导航首屏。
- 交易管理：状态筛选、金额排序，确认两条待确认记录按金额排序；桌面表格。
- 交易流程：普通态、询价展开、编辑模式、节点编辑面板打开/关闭；没有保存配置或修改完成状态。
- 管理我的：资料、邮箱、密码、偏好表单布局；没有保存或发送邮件。
- 管理权限：组合搜索/范围选择、空结果、恢复结果；本地追加浏览器回归。
- 管理通知：联系信息、订阅矩阵、设备入口；没有订阅或开启系统通知。
- 融资仪表盘：生产首屏指标与筛选区。
- 市场热点：生产页面内容与控件树已读取，详情交互尚未完成。
- 市场点评视觉/文字版：初轮审查发现窄桌面内容裁切，需要主代理继续确认并修复。

## 后续优先级

1. 市场点评：复核一级发行/二级行情窄列裁切、指数名称省略；保留已确认报告布局与导出约定。
2. 融资仪表盘：监管面板撑高同排两张指标卡，首屏空白过多；检查与项目、额度、日历的阅读顺序。
3. 交易流程：桌面询价金额占位文字偏窄，编辑面板开启后主图的可达性继续复查。
4. 逐页补齐政策/跟踪点评、研究辅助、二级池、融资模型、授信各页、融资项目/SOP/客户/投资人/数据/负债周报、新闻研报详情和资金日报。
5. 管理后台仅管理员可访问的页面需隔离组件场景；测试账号的不可访问状态不冒充页面视觉验收。

未经实际检查的页面和副作用操作不记为通过。用户后续要求停止子代理，后续检查、修复与交付由主代理直接完成。

## 第二批：操作遮挡与窄桌面报告

- 第一批已通过合并组 `35513721060`，PR #35 合并提交 `a70428d23f8a6cd2e76812cd42ced6cf245b10d4`；Cloudflare 自动构建成功。已在生产融资 SOP 详情刷新，实际确认浅蓝标题栏上线。
- 生产 SOP 列表、SOP 详情和项目页右下角新增按钮与 AI 按钮重叠。桌面融资新增按钮上移到 AI 按钮上方，保留原入口与业务动作；新增真实 SOP 页面及打开、Escape 关闭、焦点恢复场景。
- 1280 桌面市场点评存在指数名省略、较长点位省略、汇总数值裁切以及发行人/债券名截断。保留四列报告架构，完整显示指数名，长机构名称在表内换行；日期、期限和金额保持连续，窄桌面关键数值仍不小于 1rem。用四个指数、非空一级发行和二级行情夹具检查，未改变报告数据与计算。
- 继续检查了生产融资项目列表与项目展开节点、SOP 模板列表和短期融资券 SOP 详情。未修改线上项目、SOP 或提醒。
- 融资仪表盘大留白、询价字段宽度以及其余尚未完成的逐页交互检查继续保留在后续清单，不计为已修复。

## 第三批：融资数据和客户表单

- 第二批 PR #36 已合并，合并组 `35515838435` 与 Cloudflare 构建成功；本轮在生产融资数据页再次确认新增月份按钮与 AI 分离。
- 生产客户搜索后正确显示银行结果，但工具栏搜索按钮另起一行；桌面调整为固定上限、可收缩的一行搜索区。客户编辑中的类型原为无表面原生 select，改为现有 NativeSelect，与其他字段一致。
- 生产融资数据两张模块卡片没有间距；桌面补齐 24px 模块间距。1280/1440 桌面财务指标改为四列，长标题仍允许换行，主值沿卡片底部对齐，备注与网格保留间距。
- 生产月度编辑弹窗缺少 management-page 作用域，既有双列表单样式失效；补回作用域，复用既有样式。内置浏览器已对照生产与本地画面确认双列金额字段和按钮完整可见。
- 本地真实组件与生产 CSS 预览已检查客户默认/编辑、财务非空/月份切换/编辑；财务使用隔离网络夹具，未提交真实业务数据。新增 CI 场景覆盖间距、窄桌面卡片、月份切换、取消及焦点恢复。完整 CI 与上线验收以随后交付记录为准。


## 第四批待验收：筛选和详情空间

- 第三批 PR #38 合并为 `71cda5f37eb4afddc75180839c64bf126d821aeb`，合并组 `35518293457` 和 Cloudflare 构建 `887af38a-508b-4adc-a921-3c02ab5bd241` 成功。内置浏览器重新加载线上融资数据及客户名单，确认卡片间距、四列指标和单行搜索上线。候选首次发现弹窗入场动画中分两次读坐标导致断言误差，改为同一帧测量后候选与严格比较通过，未放宽阈值。
- 继续检查融资仪表盘完整日历、负债详情及投资人页。日历“显示周末”正常切成七列，但预设文字截断；负债详情现金流仅一行时仍被拉高到核心信息等高，右半卡片大量空白。
- 投资人页“具体投资人情况”的债券品种和排序标签在1440桌面被压成逐字竖排；存续规模排序和私募债筛选均正常刷新图表与表格，零存续占比显示横线。拟将标签与选择框组成不可拆分控件组。
- 负债周报生产日期2026-09-20及2026-09-18均为空态，日期切换正常、导出禁用；未点击生成。真实六页报告仍待有归档日期或组件夹具验收，不能用本次空态检查代替。
- 本批已开始实现筛选标签分组、预设自适应宽度以及核心信息旁依次堆叠现金流/完整字段；尚未完成本地画面、回归场景或CI验收，不计为已交付。

## 统一交付约定（用户最新要求）

后续页面检查、修改和本地复核全部积累到 `task/desktop-ui-layout-20260920` 的同一个 PR。完成全部检查和修改后再统一生成最终截图候选、进入合并队列和做上线复查；中途不分批合并。当前本地用1280桌面确认筛选横排、长预设完整、详情卡片排列；投资人环形图补足九类机构夹具，名称保持横排、图例完整换行。排序和私募债空范围已通过内置浏览器复核。

剩余工作以实际界面验收为准：

| 页面组 | 当前证据 | 待完成 |
| --- | --- | --- |
| 交易总览/管理/流程 | 已查首屏、筛选排序、节点打开关闭；既有修复上线 | 询价窄字段与其他编辑态最终视觉复核 |
| 市场热点 | 生产初始页；已有交互测试 | 本地非空画面、范围弹窗、详情弹窗逐一目视 |
| 政策/跟踪点评 | 已有测试夹具 | 逐页画面与选择/编辑状态目视 |
| 研究辅助 | 本地缺失数据和两点曲线、指标矩阵 | 丰富历史数据画面，图例/悬浮 |
| 二级池/融资择时 | 既有非空测试 | 浏览器完整报告与日期/筛选/展开复核 |
| 授信总览/日历/周报/助手 | 初轮部分页面；既有夹具 | 所有子页正常、筛选、详情/侧栏状态目视 |
| 融资仪表盘/客户/数据/项目/SOP | 生产页面及部分弹窗已查；前三批上线 | 仪表盘图表、项目/SOP编辑全态最终复核 |
| 投资人/负债详情 | 本地九类图表、排序、筛选、空范围、重排详情已查 | CI截图最终验收 |
| 负债周报 | 生产两日期空态 | 非空六页组件夹具与桌面排版 |
| 资讯/研报/点评详情及资金日报 | 待补 | 实际详情/归档夹具和页面操作 |
| 管理/个人页 | 已查个人、通知、权限部分状态 | 管理员专属组件及全部弹窗，不扩张测试账号权限 |

本地夹具代表组件视觉和交互，不代表生产接口、写入或真实报告内容验收。兼容跳转不重复算页面；未查状态不记为通过。

研究辅助补入100个日频点、18个月度点及全部指标的合成历史后，内置浏览器发现主利率图和两张利差图在月末/月初挤出重叠日期。时间轴启用重叠刻度避让，本地1280截图确认日期分开；悬浮卡片在面板内显示，图例可切换，价格/需求指标卡标题和曲线完整。该丰富场景新增桌面截图回归，数据仅为测试夹具。

授信总览本地1280检查发现五卡“四加一”和1240px固定表宽，展开详情时机构名称被横向滚动挤出视野。现在五项指标单行，名称/主值顶部对齐，指标标签精简为“额度使用率”；排序控件复用ghost按钮，表格按内容分配列宽，真实溢出时保留键盘滚动。内置浏览器确认机构名称、完整日期、操作列及展开编辑区域同时可见。新增风险筛选/展开收起的桌面回归；现有夹具尚未覆盖全部五类授信分项编辑，后续补丰富数据，不将这次检查视为完整编辑验收。

后续唯一集中 PR 为 #39（draft）。本地检查和提交继续累积于此，不自动合并，不提前触发最终基线/上线验收。
