# 境内市场点评前后端设计与架构

## 目标与运行方式

应用由 SvelteKit 统一承载浏览器页面和边缘后端。Python JSON API 继续作为数据源，
浏览器通过同源 `/data/*` 直接访问；本地由 Vite 读取 `.env.dev` 后转发到线上同域
数据服务并完整保留 `/data` 前缀，线上 Dashboard Worker 不接管 `/data/*`。
前端使用 Svelte 5 + TypeScript、Tailwind CSS 4 + daisyUI，ECharts 继续负责定量图表；
Cloudflare Worker 通过原生 binding 访问 D1 与 Workers AI。日常开发直接在
`dashboard/` 运行 `pnpm dev`；它会先按本地高水位把远程 D1 中新增或更新的
结构化文章、关键词与热点缓存增量 upsert 到本地开发 D1；空库首次仅建立最近 100 篇
已完成特征抽取文章的有限基线，不读取全库。页面不自动生成
或归档图片，但允许用户在浏览器中按需导出 3 倍像素密度的 PNG。
文字版 `dm-report` 继续独立生成，两条链路互不依赖。

热点聚合只输入第一阶段生成的标题、summary、importance 与 keywords 证据卡片，不再
输入完整原文。所有生成调用由 AI SDK 经统一 OpenAI-compatible Gateway 适配器发送到
`dynamic/rag`，固定 `temperature=0.1`；热点使用 `reasoning_effort=low`、
`enable_thinking=true`，不设置 completion token 上限，也不发送 top-p、top-k、重复惩罚
或 seed。系统提示只描述业务判断和证据边界，Zod Schema 自动转换为标准
`response_format.json_schema`，AI SDK 在应用端校验结果。应用再依据原始证据卡片展开
explanation、drivers、assetImpacts 和 evidence，并做领域校验。
heat 由模型直接给出（0-100 分），`来源覆盖度×0.30 + 市场影响×0.25 + 新鲜度×0.20 +
证据可信度×0.15 + 跨资产关联度×0.10` 权重公式仅作为提示，应用不自行计算；单来源
热点热度不超过 60。只输出固收与权益两条影响。

页面面向日常桌面研究工作，按宽屏研究终端设计，内容最大宽度为 `2100px` 并居中。
目标视口为 `1920×1080`，页面以一屏完整展示为首要约束；`1440×900` 保留完整
四列，`1280×720` 允许横向滚动，不把手机端适配列为验收范围。用户可切换报告日、
手动刷新上游数据，并在加载或上游失败时获得明确反馈。

前端仅保留 `/` 宽屏单屏仪表盘，不提供版式切换入口。页面共享稳定的 API、
数据口径、报告日期与导出能力。

## 视觉方向

整体采用克制、清晰的机构级数字驾驶舱风格：

- 页面底色固定为 `#f6f8fb`，卡片为白色，正文为 `#172033/#273142`，使用
  `#d8e2f0` 轻边框、`8–10px` 圆角和低阴影；除圆形图标和胶囊标签外，
  面板、内层卡片和控件统一使用 `6/8/10px` 三级圆角令牌，避免出现突兀直角。
- 品牌蓝 `#2f6fd6` 作为界面主轴和栏目编号色，橙色 `#ff7a45` 只用于日期等
  少量重点提示；数据涨跌仍保留红涨绿跌语义。
- 标题、正文、控件和数字统一使用操作系统默认无衬线字体；金额、收益率、日期和
  表格数值只通过 `font-variant-numeric: tabular-nums` 对齐数字，不切换另一套字体。
- 页面主标题与核心 KPI 保持更强层级，其余正文、表格、图表辅助、控件和小字统一
  不低于 `1rem`；所有卡片的首行与末行均为 `1rem/1.2`，中间主值为
  `1.25rem/1.5`，顶部核心卡主值为 `1.5rem/1.5`。
- 权益和收益率上升用红色、下降用绿色；OMO 净投放用绿色、净回笼用红色。
- 红绿只用于实际数据方向，不用于栏目序号或其他装饰；`01–07` 栏目编号采用
  统一蓝色编号块。
- 页面内容区占满可用窗口宽度，只保留窄页边距；核心指标独占整行，主体采用
  `grid-template-columns: repeat(4, 1fr)` 的四列两行等宽信息矩阵，不使用长报告式
  纵向滚动。
- 页面不使用无业务含义的英文眉题或装饰文案。
- 主标题固定为“资金管理部 • 市场点评”，使用紧凑的浅蓝渐变抬头带和主题蓝
  细竖线，不增加既有抬头行高度；“资金管理部”为前缀层级，“市场点评”为主标题。
  报告日期显示为“2026年7月24日 星期五”的单行格式，日期置于浅色日期框，
  星期使用主题蓝色块标签。
- 顶部保留报告日期以及 OMO、DR007、10Y 国债、上证指数、同业发行、债券成交
  六个指标，上证指数位于同业发行之前；不展示数据汇集时间。同业发行展示
  T-1 至 T 两个交易日的合计规模，
  下方只显示其相对 T-2 至 T-1 窗口的整数变动额，不显示“较昨日”；债券成交从
  东财存量债当日成交中统计成交债券只数，不再逐条展示成交记录，没有数据时显示
  “今日暂无”。
- 六项指标使用不同业务色的圆形线性 SVG 图标和 4%–8% 浅色底，卡片只保留
  名称、主值和变化值三层，不使用高饱和红绿整卡背景。
- 所有卡片使用 `justify-content: space-evenly`，子类型不覆盖该规则。顶部卡片按
  `3rem` SVG 图标、居中数据区和等宽空白占位三段布局；卡片 SVG 不设置 padding。
- 三类指标统一复用 `.card`、`.card__content`、`.card__label`、`.card__value` 和
  `.card__detail` 组件类；`.core-card`、`.indicator`、`.stat-card` 只保留业务
  语义，不再各自定义重复的结构和字号样式。顶部和资金区使用
  `.card--segmented`，资金指标额外使用 `.card--centered`。
- 顶部工具栏不重复展示品牌名称或品牌图标，只保留报告日、刷新和高 DPI 图片导出。
- 图片导出在前端完成，固定为 3 倍像素密度 PNG，只包含报告正文，不包含顶部工具栏。
- 主体严格按从左到右、从上到下的 1–8 格理解：第一行为今日聚焦、公开市场操作、
  固收市场、权益市场，第二行为一级发行、二级行情、东财债券、权益行业热力图。
  第 4 与第 8 格纵向合并为一个连续的权益面板，不使用边框或独立标题把指数区与
  行业热力图区割裂。
- 今日聚焦默认为空白的无工具栏富文本编辑区，内容按报告日保存在本机浏览器中；
  编辑区隐藏滚动条外观但保留滚动能力，不增加额外边距，支持
  `Ctrl/Command+B` 加粗、`Ctrl/Command+I` 斜体、`Ctrl/Command+U` 下划线和
  `Ctrl/Command+Shift+H` 将所选文字设为主题蓝强调色，不添加背景。一级发行只显示明细列表和底部窗口规模
  汇总，不显示发行结构；二级行情只显示最接近
  1Y、2Y、3Y、5Y 的四只可比公募证券公司债，表格不显示实际剩余期限列，也不显示
  期限梯度摘要；东财债券继续显示存量债期限结构图。
- 公开市场操作图表不重复显示标题和单位；所有数值标签位于柱体外，与柱端保持
  相同距离，绘图区尽量占满卡片。图表下方展示当日、近 5 日和近 10 日净额汇总。
  汇总数值使用正负号表达方向：净投放显示 `+`，净回笼显示 `−`，不重复输出
  “净投放/净回笼”文字。
- 国债昨日与今日曲线的 tooltip 和标签保持真实收益率；图上按当日曲线跨度与
  最大日变动自动选择 `6–18` 倍分离系数，等比例放大昨日线相对今日线的垂直间距，
  使两条接近的曲线可辨。今日曲线使用主题蓝，昨日曲线使用较宽的中性灰虚线，
  图例位于图表区右下方、横轴上方。
  今日数值
  标签第一行以大号黑字显示
  收益率，第二行显示当日变动 bp，并按红涨绿跌着色。标签整体根据当日变动方向
  确定优先侧，再对上、下、左右及四个斜向候选位置评分，自动避开
  图例、坐标轴、画布边界和已经放置的标签。图例与绘图区之间只保留必要间距，
  数据标签使用透明背景，不添加白底。
- 行业热力图不显示“行业全景”标题、数据日期和涨跌图例，也不使用白底、白色
  分隔缝、深色容器或黑色分隔线；行业矩形无缝铺满可用区域，并使用带最低饱和度
  的红绿渐变，避免小幅涨跌色块过淡。
- A 股四个主要指数使用带红绿区间和方向指针的仪表盘；红色代表上涨方向，绿色
  代表下跌方向。仪表盘采用更大、更紧凑的细弧渐进色带、轻量刻度、细指针和
  带阴影中心锚点，四个表盘尽量填满卡片，避免大面积无效空白。
  指数名称由 HTML 放在每个表盘图形下方，不绘制在表盘内部；放大的点数和
  涨跌幅放在每个仪表盘右侧，点数不附加“点”字，避免名称、指针和值互相重叠。
  仪表盘区约占权益内容高度的 `31%`，指数名称向表盘靠拢，压缩出的纵向空间优先
  分配给行业热力图。
  成交额和融资融券
  余额不显示行情日期，使用各自的大尺寸圆形线性图标，并与顶部核心数据使用相同
  的弹性 flex 卡片结构。
- 仪表盘维持表盘靠左、点数和涨跌幅在右的紧凑布局，并在窄卡片下使用独立的
  紧凑坐标。
- 顶部核心数据、成交额和融资融券余额的 SVG 图标统一放大至 `3rem`、取消
  padding 并保留加粗线条。
- 一级发行列表使用类型、日期、发行人、期限、规模和票息六列，表头固定，所有列
  居中并按内容自适应列宽，票息保留两位小数；数据较多时仅在格内滚动，不推动
  整页产生纵向滚动；底部只保留窗口发行规模汇总。
- 二级行情列表使用期限桶、债券、发行人和成交收益率四列，所有列居中并按内容
  自适应列宽，期限桶使用与一级发行类型一致的色块标签，成交收益率保留两位小数；
  四个期限桶按
  1Y、2Y、3Y、5Y 顺序各选择一只距离目标期限最近且不重复的债券，底部保留
  有效样本和收益率区间。
- 可比证券公司债纳入当日剩余期限不超过 5 年、且有成交收益率的公募债，按剩余
  期限排序，排除发行人为东方财富证券的债券。不查询、不要求、不展示中债估值。
  进入期限桶筛选与汇总前，使用 Theil-Sen 稳健期限趋势和残差 MAD 统一剔除
  收益率异常值，避免极端成交扭曲结果。
- 东财存量债的估值线和估值点使用同一蓝色，成交点使用更大、更醒目的红色强调。
  成交收益率标签根据成交点相对估值线的位置自动放到线条反方向，并使用白底避免
  与线条重合，成交收益率保留两位小数。具体债券名称、期限、估值与成交收益率
  通过 tooltip 按需查看，
  页面不再附加存量明细表。
- 报告不显示页脚。
- 面板标题只显示一行标题，不自动添加副标题或口径行。
- 图表坐标、图例、辅助标签、表格和普通小字均不小于 `1rem`。
- 必要的辅助信息、数据日期、空状态、图例和坐标轴使用 `#667069` 或更深颜色；
  `#8d9690` 只用于非必要装饰或禁用状态。
- 图表与指标卡使用轻边框、适度圆角和克制阴影，保持机构研究报告的清晰感，
  避免线框稿式的硬边框和大面积无效留白。
- 同一行模块必须上下边线平齐；布局使用拉伸等高与自适应图表高度，不用固定
  像素高度制造空白。ECharts 容器及其 SVG/Canvas 必须始终占满图表区高度，
  根 `<g>` 使用全画布透明边界并覆盖完整 SVG；坐标系外边距只保留标签所需空间。
  东财债券图例与绘图区紧邻，尤其是公开市场操作、国债和东财债券图表不得出现
  未使用空白。
- 资金行情四项指标以四张等宽、等高且间距一致的浅色圆角卡片排列，容器不再使用
  整块浅色底或无间距分段；标签、数值和变动全部水平居中。
- 图表 tooltip 限制在图表内部，最大宽度 260px，并允许长名称换行。
- 动效只用于加载状态和轻量悬停，主要图表动画不超过 `300ms`，并尊重
  `prefers-reduced-motion`。

## 页面结构

1. 顶部本地工具栏：报告日、刷新数据、导出高清图片。
2. 报告抬头：主标题、日期/星期，以及横向排列的六个核心指标。
3. 第一行四列：今日聚焦、公开市场操作、固收市场、权益市场上半区。
4. 第二行四列：一级发行列表、二级行情四期限列表、东财债券图、权益行业热力图。
   权益市场跨越第 4、8 格，为一个无分割的完整面板。

## 数据口径

- 页面通过同源 `/data/report?date=YYYY-MM-DD` 获取结构化数据，不接触
  `.env`、Cookie 或 Choice 凭据。
- 服务端按报告日缓存结果；“刷新数据”通过 `refresh=1` 强制重新取数。
- 视觉报告直接消费 DM、东方财富与 Choice 接口，不读取文字报告归档。
- 上证指数、深证成指、创业板指和科创 50 全部使用 Choice `csd` 的 `CLOSE` 与
  `PCTCHANGE` 指标，不再解析东方财富收盘文章，也不使用东方财富实时行情兜底；
  报告日数据不完整时回退至此前最近一个四项指数均有完整数据的交易日。
- 两融数据使用东方财富 JSON 接口。
- 沪深京成交额使用 Choice `800004.EI` 的 `AMOUNT` 指标，原始单位为元，页面统一
  除以 `1e8` 展示为亿元；报告日尚无数据时取此前最近一个有值交易日，并以最近
  两个有值交易日计算变动额。
- 行业矩形树图仅使用 31 个申万一级行业，不展示成分股。矩形面积严格按
  行业总市值分配，颜色表达涨跌幅。
- 行业标签根据每个矩形的实际宽、高和面积连续计算字号，面积越大的板块字号越大，
  字号范围为 `10px–1.75rem`。每个可见标签必须同时完整显示行业名和涨跌幅；
  若矩形无法容纳两行 `10px` 文字，则整体隐藏标签，不拆字、不省略其中一行。
  完整行业数据始终可由 tooltip 查看。
- 行业数据使用 Choice `csd` 的 `PCTCHANGE` 和 `MV`。报告日数据不完整时，
  回退至此前最近一个 31 个行业均有两个字段的交易日；实际数据日期保留在
  API 数据契约中供校验，不在热力图上额外展示。
- 一级发行、二级成交和东财存量债筛选口径与文字报告共享业务函数。
- 同业发行核心指标以 Choice 返回的真实交易日序列确定 T、T-1、T-2：当前值为
  T-1 至 T 两个交易日发行规模之和，变动额为该窗口减去 T-2 至 T-1 窗口。
- 一级发行直接展示完整业务字段列表。
- 可比证券公司债按 1Y、2Y、3Y、5Y 目标期限挑选最接近的四只债券。
- 东财存量债是离散债券点，不使用平滑插值；估值采用蓝线蓝点，成交采用更大的
  红点并在空间允许时标注成交收益率。

## 模块边界

- `api/models.py`：稳定领域模型和 JSON 序列化。
- `api/collectors.py`：DM/东方财富源数据归一化，不负责 HTTP 服务。
- `api/industry.py`：Choice 指数、成交额、行业查询和完整日期选择。
- `api/service.py`：一次报告快照的用例编排和客户端生命周期。
- `api/server.py`：本地 JSON HTTP、缓存和错误边界，不提供静态资源。
- `src/app.html`：SvelteKit HTML 外壳和页面元信息。
- `src/styles.css`：既有报告版式、响应式、颜色语义和动效。
- `src/App.svelte`：报告页面状态、加载/错误/导出流程和视图组合。
- `src/routes/`：页面路由和 `/api/rag/*` 应用接口；不实现 `/data/*` 后端代理。
- `src/lib/server/ai-gateway.ts`：AI SDK、Dynamic Routing、认证和协议兼容的唯一调用边界。
- `src/lib/server/hotspots.ts`：D1 证据读取、结构化 AI 聚合、确定性校验和缓存。
- `src/lib/components/WordCloud.svelte`：`d3-cloud` 布局与可交互 SVG 词云。
- `src/components/`：页面使用的 Svelte 指标卡、表格、编辑器、汇总条和
  ECharts 宿主组件。
- `src/view-model.ts`：纯展示计算，不操作 DOM。
- `src/app.css`：Tailwind CSS 与 daisyUI 入口；既有设计 token、布局和图表
  几何仍由 `styles.css` 决定，组件库不得改变视觉契约。
- `src/api.ts`：报告前端的同源数据访问边界。
- `src/export.ts`：浏览器端 3 倍像素密度 PNG 导出，不负责数据获取。
- `src/charts/`：按市场域拆分的 ECharts 配置与统一实例生命周期。
- `src/formatters.ts`：统一数值、日期和涨跌格式。

数据依赖方向为：线上 `browser -> /data -> Python API`，本地
`browser -> Vite /data proxy -> https://eastmoney.hasbai.xyz/data`；线上热点依赖方向为
`browser -> /api/rag/hotspots -> D1 structured features -> AI Gateway dynamic/rag -> hotspot_cache`。
本地热点在启动时只读增量同步上述 D1 结构化数据，再以相同接口逻辑读取本地副本。
Python 不读取、打包或提供前端资源，浏览器不导入 Python 实现。

## 安全与可访问性

- 默认仅监听 `127.0.0.1`，除非用户显式传入其他 `--host`。
- API 对未知路由返回 JSON 404，响应设置 `X-Content-Type-Options`。
- 前端依赖由 pnpm 锁定并本地打包，不使用外部 CDN 或内联脚本。
- API 错误只返回必要消息，不返回堆栈、Cookie、Token 或签名 URL。
- 页面使用语义标题、可访问表单标签、图表 `aria-label` 和 ECharts ARIA。
- OMO 的 10 个数值标签必须全部位于柱体外，桌面视口下不得互相重叠。
- 图表坐标、图例和辅助标签统一不低于 `1rem`，关键数据标签按需要放大；普通
  网格线为浅灰虚线，零轴使用更深的实线；tooltip 正文不小于 `12px`。
- 系统启用 `prefers-reduced-motion: reduce` 时，同时关闭 CSS 和 ECharts 动画。

## 验收标准

- `pnpm dev` 从 `.env.dev` 加载远程数据源并启动 SvelteKit 应用；`/data/health`
  保留前缀转发并返回 `{"status":"ok"}`。
- `/dashboard` 可加载完整报告，页面不显示版式切换入口。
- `/data/report` 返回完整 JSON，包含 31 个行业以及行业实际数据日期。
- `/api/rag/hotspots` 默认滚动读取最近 20 篇文章，也支持开始/结束日期；同一范围与
  证据指纹复用 D1 缓存，`refresh=1` 才强制重新聚合，结果固定为 8–15 个热点。
  `/rag/hotspots` 使用全屏词云，右上角弹出面板切换滚动篇数/日期范围，点击或键盘
  激活关键词以覆盖层查看解释、固收/权益影响和证据，不改变词云宽度与布局。
- `1920×1080` 桌面视口下无需纵向滚动即可看到六个核心指标和完整 1–8 格；
  第 4、8 格合并为贯穿两行的权益市场，行业热力图位于其下半区。
- `1440×900` 继续保留四列，正文和表格不低于 `1rem`，无文字重叠或图例裁切；
  `1280×720` 可通过横向滚动访问完整权益列，核心 KPI、聚焦、公开市场、固收和
  权益均可访问。
- 今日聚焦可直接编辑且按报告日保存；一级发行只有列表，二级行情只出现
  1Y、2Y、3Y、5Y 四个期限桶；东财债券保留期限结构图。
- 一级发行和二级行情不显示结构或梯度摘要，不以拉高表格行填充空白。
- 图表绘图区充分使用卡片宽度，无无效的大块左右留白；国债曲线首尾标签完整，
  行业标签只允许“行业名 + 涨跌幅”同时完整显示或整体隐藏，不允许叠字、拆字、
  省略号或只显示其中一行。
- 切换日期、刷新、加载、错误重试和 3 倍像素密度 PNG 导出均可用；点击导出时
  页面不得发生纵向位移，导出图片不包含顶部工具栏，浏览器控制台无错误。
- Svelte/TypeScript 类型检查、单元测试、SvelteKit 构建、Wrangler dry-run 与关键
  接口请求全部通过；本项目不以浏览器或截图视觉检查作为验收步骤。
